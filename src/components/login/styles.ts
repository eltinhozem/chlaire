
import { styled } from 'styled-components';
import { LoginButton as StandardLoginButton } from '../buttons';

// Use the standardized LoginButton from the buttons folder
export const Loginbutton = StandardLoginButton;
